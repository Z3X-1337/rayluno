# Rayluno Security Model

Rayluno treats natural-language input and model output as untrusted. The model may propose only registered skills and validated arguments. Consequential skills require explicit confirmation, and execution produces a receipt.

The product does not promise unrestricted command execution. Broad capability is achieved through composable, reviewable skills rather than an open shell.
